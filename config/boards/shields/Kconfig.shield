# No whitespace after the comma or in your part name!
config SHIELD_LEFTRIGHT_LEFT
    def_bool $(shields_list_contains,leftright_left)

# No whitespace after the comma or in your part name!
config SHIELD_LEFTRIGHT_RIGHT
    def_bool $(shields_list_contains,leftright_right)